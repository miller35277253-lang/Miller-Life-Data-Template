# REPORT_TEMPLATE.md —— 日报固定样式（唯一权威模板）

> 本文档是**早报 / 晚报的唯一样式模板**。任何一次日报（无论早报 09:00 还是晚报 22:00、无论手动还是定时），都必须**完全按照下面的版式结构输出**，不得发明新板块、新标题、新顺序。
>
> **锁定规则（锁结构、留措辞）**：所有 `##`/`###`/板块顺序/编号/列表符号/字段名**固定**，不可增删改序；每处文案的具体措辞允许在保持语义的前提下微调语气（更口语/更正式），但**不允许改结构**。
>
> **动态原则（强制）**：当天没有数据的板块**整体不出现**（一个也不留空），有数据才出现；「今日/明日安排」板块例外——无安排时也要出现，内容写"暂无安排"。

---

## 早报 ·（09:00）

> **天数计算**：从 `个人档案.json` 读取 `birthday`，计算「今天 - 生日 = 来到世界第 N 天」。N = 当天日期 - 出生日期（算头不算尾或算尾不算头都行，统一用整日差）。

```
早安，(称呼｜可用时间/星期做开场)。来到世界第 N 天。先看昨天，再给你今天。

## 昨日汇总
> 由渲染脚本生成，取【昨天】实际有数据（无数据的板块整体不出现）

> 呈现方式：渲染卡片以**内联可视化/标准卡片**呈现在对话中（数据以卡片、表格、文字呈现，遵循 可视化规范.md 的卡片与配色）。**禁止把渲染脚本的原始 HTML / CSS / JS 源码复制粘贴进对话**；只展示卡片效果，不展示代码。

## 今日安排
> 今天到期的任务 / 里程碑 / 提醒 / 对话里提过的带日期事项，逐条列出
> 若今天没有任何安排，则这一条写：「今日暂无安排」

- [ ] 事项 1（含时间点，若有）
- [ ] 事项 2
…

## 收集箱
> 由渲染脚本生成，显示当前活跃的收集箱条目（待处理的想法/灵感/待分类内容），无数据则整个板块隐藏
> 呈现方式：渲染卡片，每条显示内容摘要，标注条数

## 晨间确认（交互式，逐条问，等用户回答后再问下一条，不一次抛完）
1. 今天有没有要记录/安排的？（有→记录为 task/reminder；没有→下一问）
2. 今天几点起床？（有→记 activity 睡眠时段（起床）；没有→结束）
> 注：按用户规则 R005，禁止询问昨天的入睡时间、步数等已录入数据，用户主动提供时再记录。

---
```

---

## 晚报 ·（22:00）

> **天数计算**：同早报，从 `个人档案.json` 读取 `birthday`，计算当天 N。

### 晚安，(称呼)。来到世界第 N 天。今天收个账，顺便看下明天。

### 今日汇总
↓ 由渲染器取【今天】数据，只统计当天实际有的板块

> 呈现方式：同早报，以**内联可视化/标准卡片**呈现（数据以卡片、表格、文字呈现）。**禁止把渲染脚本的原始 HTML / CSS / JS 源码复制粘贴进对话**；只展示卡片效果，不展示代码。

### 明日安排
- 查询明天到期的提醒 / 里程碑 / 开始项目 / 对话提过的带日期安排，逐条列出
- 若明天没有任何安排 → 不列此块，直接跳过
- 列完顺带问一句：明天有没有想提前记下的？

### 收尾
（一句话结束语，语气可自由）

---

## 附注：填空时的硬约束

- **数据唯一来源**：数值一律取自 `node tools/渲染今日.mjs --mode morning|evening --out stdout` 的输出结果 / 对应 data 记录。禁止脑补、禁止四舍五入幅度、禁止补 0。
- **没有数据的板块**：早报/晚报都按"当天无则此板块整体不显示"，绝不补假值。
- **「今日/明日安排」唯一来源**：`node tools/数据工具.mjs due <日期>` 或渲染器安排卡输出；若命令失败，必须写明「安排计算失败」，禁止自行估算清单。
- **时区**：一律用北京时间 `+08:00`，不用服务器 UTC。
- **写作**：所有文案中文；结构严格照模板；措辞可微调；不写死板块标题里没有的板块。
- **只呈现效果，不呈现源码**：任何板块的卡片/图表都以**内联可视化/标准卡片**方式呈现（卡片、表格、文字），**禁止把渲染脚本或工具输出的原始 HTML / CSS / JS / JSON / 命令输出复制粘贴进对话**。
- **写入数据**：只有用户确认后才写；写后立即 commit + push 到 GitHub。
