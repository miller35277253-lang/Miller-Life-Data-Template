# AGENTS.md — 系统总入口与操作地图（强制约定）

> ## ⚠️ 提交铁律（最高优先级，所有 AI 必须遵守）
> 1. **所有 git 提交必须通过工具执行**：`node 能力/提交并确认.mjs "提交信息"`
> 2. **禁止直接使用** `git add`、`git commit`、`git push` 命令
> 3. 工具返回结果的第一行是强制指令，**必须将完整输出原样返回给用户**，不得修改、删减或省略
> 4. **提交工具内置ID连续性强制检查**：新增记录的ID必须是该类型当天最大序号+1，且类型必须已注册（有类型配置）。不通过则阻止提交，提示用 `node 能力/数据工具.mjs nextid <类型>` 生成正确ID。
> 5. 违反此规则视为严重错误
>
> ## 📋 AI 的强制操作要求（任何数据操作前必须走完）
>
> **必须做：**
> 0. 先读本文件（AGENTS.md）。
> 1. 当前上下文不包含用户规则时，必须读根目录 `用户规则.json`。
> 2. 列 `数据/` 目录，读中文文件夹名。
> 3. 进对应文件夹，读「*：类型配置.json」。
> 4. 按必读文件的标准执行（写入数据的完整流程见 §3「八步闭环」；历史记录读取要求以八步闭环第1步为唯一权威，不在别处重复）。
>
> **禁止做：**
> - 禁止不读 AGENTS.md 就操作数据。
> - 禁止不列目录、凭记忆猜英文名。
> - 禁止绕过中文名用别的标识符当入口。
> - 禁止不读必读文件就凭想象写字段。
>
> 本文件是整个项目对 Agent 的**总入口**，存放在 Gitee 私有仓库 `Miller-Life-Data/Miller-Life-Data`（https://gitee.com/Miller-Life-Data/Miller-Life-Data）。
>
> **任何一次会话，无论由谁（含新开会话、换成别的模型）驱动，第一步必须：**
> 1. 如果工作区还没 clone → `git clone https://gitee.com/Miller-Life-Data/Miller-Life-Data`
> 2. 如果工作区已有 → `git pull origin main` 同步最新
> 3. 再按下面的顺序读取文档，再开始处理任何与数据、可视化、任务相关的事。
>
> 目的：让系统**完全靠文件本身自启动、自对齐**，不依赖任何一次对话的上下文记忆。

***

## 0. 会话启动协议（强制，先读后动）

### 0.0 强制启动检查（双层保险，客户端自定义指令和本文件都有此指令）

每次会话开始，**必须先执行以下检查，禁止跳过**：

1. 检查当前上下文末尾是否包含「规则加载验证」标记
2. 如果有标记，提取标记里的规则哈希值
3. 运行 `cd /sandboxdata/workspace/file/Miller-Life-Data && node 能力/生成系统提示词.mjs --hash` 拿到当前规则的最新哈希
4. 两个哈希一致 → 规则已加载且最新，直接开始工作，**禁止重复加载**
5. 没有标记，或哈希不一致 → **必须运行** `cd /sandboxdata/workspace/file/Miller-Life-Data && node 能力/生成系统提示词.mjs` 加载完整规则
6. 加载完成后必须能在上下文中看到验证标记，否则视为失败，重新加载
7. **禁止在没有完整规则的情况下处理任何用户请求**

---

每次会话开始，**禁止跳过以下读取顺序**：

1. **本文件（AGENTS.md）** — 系统级规则 + 系统地图 + 操作影响矩阵。**优先级最高，未读完禁止处理任何数据。**
2. **`用户规则.json`** — 用户个人操作规则（当前生效）：录入习惯、汇报偏好、预算规则、可视化偏好。**未读完禁止录入任何带时间的记录，用户改了就直接更新，不保留历史。**
3. **`个人档案.json`** — 用户个人档案（基本信息，根目录）。
4. **各数据文件夹内的「*：类型配置.json」** — 每个数据类型文件夹里都有一个类型配置文件（如 `数据/收支记录：所有花钱和收入的记录/收支记录：类型配置.json`）。**操作任何数据前，禁止不读对应文件夹的类型配置就开始写入。**
5. **`能力/可视化规范.md`** — 可视化的唯一权威规范。做任何图表、日报、总览前必读。
6. **`能力/报表模板.md`** — 早报 / 晚报唯一固定模板。**生成任何日报/总览前必读**。

> 若上述任一文件缺失且不影响本次任务，可继续；若某文件存在但内容与本次任务冲突，以本文件（系统规则）、`用户规则.json`（用户偏好）、各数据文件夹内的「*：类型配置.json」（数据定义）为准。

***

## 1. 系统地图（文件存放位置）

**本系统只有四类顶层位置，记住它们的分工即可，具体有哪些文件自己列目录看：**

| 位置 | 放什么 |
|---|---|
| **根目录** | 系统核心文件：`AGENTS.md`（本文件）、`用户规则.json`（开工前必读）、`个人档案.json`、`README.md` |
| **`数据/`** | 用户自己产生的所有数据，每个类型一个中文「名称：备注」文件夹，文件夹内有类型配置和数据文件 |
| **`能力/`** | 系统的所有能力：可执行脚本（.mjs）、规范文档（.md）、运行状态。按模块归类到子文件夹，通用能力直接放根目录 |
| **`收藏资料：我收藏的别人的东西，谱子诗歌文章歌词网页等/`** | 用户收藏的别人的东西（别人的歌词、谱子、文章），和用户自己产生的数据分开 |

**需要知道有哪些能力/数据类型时，直接列对应目录看实际的中文文件夹名，禁止凭记忆猜测。**

### 1.1 数据目录（`数据/`）的组织规则

每个数据类型一个中文「名称：备注」文件夹，文件夹内必有一个「*：类型配置.json」（该类型的字段规范和操作规则，操作前必读）+ 该类型的数据文件。多数类型数据文件为 `<名称>.jsonl`（每行一条），单实体类型（人物、任务、项目、收集箱活跃条目、配置）为单个JSON。

**类型清单不在本文档维护**——数据类型是动态增减的，需要知道当前有哪些类型时，**直接列 `数据/` 目录**，或运行 `node 能力/数据工具.mjs types`。

### 1.2 能力目录（`能力/`）的组织规则

能力按模块归类到子文件夹，每个模块包含相关的脚本、规范、状态文件。例如 `能力/数据检查/` 包含数据检查的所有相关文件。通用能力直接放 `能力/` 根目录。需要用什么能力时直接列目录找，不在此逐一登记。

***

## 2. 关联规则（动态，不预设穷举）

### 2.0 通用原则（最高优先级）

**关联不预设、不限制类型**——任何两条数据之间都可能有关联意义。录入时按以下规则处理：

1. **录入时主动判断**：写入任何数据时，查看当前数据结构，判断是否需要与其他记录建立关联（如一条支出关联了某个项目、一首诗关联了某个人物、一篇收藏关联了某个活动）。
2. **单向发生必须双向同步**：只要在A记录里写了指向B的关联字段（如 `projectId`、`peopleIds`、`archivedFromTask`），必须同时在B记录里补上指向A的反向关联（如 `project.activityIds`、`person.expenseIds`、`task.meta.completedActivityId`）。**禁止只写一头。**
3. **反向字段不存在就直接添加**：如果B记录里没有对应的反向字段，直接在B记录的数据里添加该字段，不需要更新类型配置。字段是动态的，类型配置只放核心约束。
4. **修改/删除时同步**：修改或删除一条记录的关联字段时，必须同步更新对端的反向关联。
5. **验证**：任何关联操作完成后，跑 `node 能力/数据检查/关联检查.mjs` 确认无悬空、无遗漏。

> 下方 2.1-2.4 是**常见关联的参考清单**，不是穷举。遇到清单外的关联，按上述通用原则处理。

### 2.1 常见关联参考（新增数据时）

| 新增类型 | 如果包含此字段 | 必须同步更新 | 更新方式 |
|---|---|---|---|
| activity | `peopleIds` | 对应person的`activityIds` | 追加ID，逗号分隔 |
| activity | `projectId` | 对应project的`activityIds` | 追加ID，逗号分隔 |
| activity | `meta.archivedFromTask` | 对应task的`meta.completedActivityId` | 写入activity ID |
| task | `peopleIds` | 对应person的`taskIds` | 追加ID，逗号分隔 |
| task | `projectId` | 对应project的`taskIds` | 追加ID，逗号分隔 |
| intake | `peopleIds` | 对应person的`intakeIds` | 追加ID，逗号分隔 |
| poem | `peopleIds` | 对应person的`poemIds` | 追加ID，逗号分隔 |
| diary | `peopleIds` | 对应person的`diaryIds` | 追加ID，逗号分隔 |
| diary | `projectId` | 对应project的`diaryIds` | 追加ID，逗号分隔 |
| expense | `peopleIds` | 对应person的`expenseIds` | 追加ID，逗号分隔 |
| item | `peopleIds` | 对应person的`itemIds` | 追加ID，逗号分隔 |
| lyric | `peopleIds` | 对应person的`lyricIds` | 追加ID，逗号分隔 |
| inbox | `peopleIds` | 对应person的`inboxIds` | 追加ID，逗号分隔 |
| inbox | `projectId` | 对应project的`inboxIds` | 追加ID，逗号分隔 |

### 2.2 修改数据时

| 修改场景 | 必须检查 | 操作 |
|---|---|---|
| 修改activity的`peopleIds` | 旧person和新person的`activityIds` | 从旧person移除，向新person追加 |
| 修改activity的`projectId` | 旧project和新project的`activityIds` | 从旧project移除，向新project追加 |
| 修改task的`peopleIds` | 旧person和新person的`taskIds` | 从旧person移除，向新person追加 |
| 修改task的`projectId` | 旧project和新project的`taskIds` | 从旧project移除，向新project追加 |
| 修改task的`status`为completed | activity、completed.jsonl、原文件 | 用`归档任务.mjs`一键完成，禁止手动分步 |
| 修改inbox的`projectId` | 旧project和新project的`inboxIds` | 从旧project移除，向新project追加 |
| 修改任何记录的`time` | JSONL文件排序 | 重新按time倒序排列整个文件 |
| 修改person的`name`或`relation` | 无 | person是单文件，直接改，不影响关联（关联用ID不用名字） |
| 修改project的`name` | 无 | project是单文件，直接改，不影响关联（关联用ID不用名字） |

### 2.3 删除数据时

| 删除类型 | 必须同步清理 | 操作 |
|---|---|---|
| 删除activity | 所有person的`activityIds`、所有project的`activityIds`、指向它的task的`meta.completedActivityId` | 从所有反向关联中移除该ID；如果有task指向它，先解除或警告 |
| 删除task（活跃） | 所有person的`taskIds`、所有project的`taskIds` | 从所有反向关联中移除该ID；如果是完成态，用归档而非删除 |
| 删除person | 所有关联数据的`peopleIds`（activity/task/intake/poem/diary/expense/item/lyric/inbox） | 从所有数据的peopleIds中移除该person ID；**删除前必须确认**，因为影响面大 |
| 删除project | 所有关联数据的`projectId`（activity/task/inbox/diary） | 从所有数据的projectId中移除或置空；**删除前必须确认**，因为影响面大 |
| 删除inbox | 对应project的`inboxIds`、对应person的`inboxIds` | 从反向关联中移除；用转化而非直接删除（转化会自动清理） |
| 删除intake/poem/diary/expense/item/lyric | 对应person的反向关联字段 | 从person的对应Ids字段中移除 |

### 2.4 任务归档的完整影响链（用归档任务.mjs自动完成）

```
用户说"任务完成了"
  → 1. 创建一条activity记录（activity=任务标题，meta.archivedFromTask=taskId）
  → 2. 更新task的status=completed
  → 3. 将task追加到 待办任务：已归档/已完成.jsonl
  → 4. 删除 tasks/ 下的原task文件
  → 5. 更新person.activityIds（如果task有peopleIds）
  → 6. 更新project.activityIds（如果task有projectId）
  → 7. 更新person.taskIds（移除该taskId）
  → 8. 更新project.taskIds（移除该taskId）
  → 9. 运行 能力/数据检查/关联检查.mjs 验证
```

**禁止手动分步归档**，必须用 `node 能力/归档任务.mjs <taskId>`，工具自带失败回滚。

### 2.5 收集箱转化的完整影响链（用转化收集箱.mjs自动完成）

```
用户说"把这个收集箱条目转成任务/活动"
  → 1. 在目标位置创建新记录（task或activity）
  → 2. 更新person反向关联（如果有peopleIds）
  → 3. 更新project反向关联（如果有projectId）
  → 4. 从原project.inboxIds / person.inboxIds中移除该inbox ID
  → 5. 将inbox追加到 收集箱：已转化/已转化.jsonl
  → 6. 删除 收集箱/收集箱：活跃/ 下的原文件
  → 7. 运行 能力/数据检查/关联检查.mjs 验证
```

***

## 3. 写法律（本系统最重要的安全边界）

* **用户要求查询数据（全量召回，禁止部分读取）**：用户问"今天有什么活动""我上周花了多少钱""查一下X记录"等查询类问题时，**必须用 `node 能力/数据工具.mjs query <类型key> --from <YYYY-MM-DD> [--to <YYYY-MM-DD>]` 全量查询**，禁止只 Read 文件前几条就回答，禁止用 grep 搜关键词后只看匹配行就回答。query 工具会自动筛选时间范围内的所有记录（含跨天活动），输出精简列表，确保不遗漏。需要查多个类型时，对每个类型分别调用 query。不知道类型 key 时先跑 `node 能力/数据工具.mjs types` 查看。
* **用户主动要求录入数据**：Agent 必须执行**八步闭环**，缺一不可：
  0. **保存聊天记录（唯一入口：保存指令工具，采用滞后存储）**：**禁止直接编辑聊天记录文件**。保存指令工具支持两种模式：
     - **command模式**（保存用户输入）：`node 能力/保存指令.mjs "用户原话"`
     - **reply模式**（保存AI回复，滞后存储）：`node 能力/保存指令.mjs --reply "上一轮AI回复内容" --command-id <上一轮的commandId>`
     
     **每一轮的保存时序（滞后存储，必须严格遵守）：**
     - **第一轮对话**（没有上一轮回复）：直接调用command模式保存当前轮用户输入，拿到commandId
     - **非第一轮对话**：
       1. 先从系统上下文里拿到**上一轮AI的回复内容**
       2. 调用reply模式保存上一轮AI回复：`node 能力/保存指令.mjs --reply "上一轮回复内容" --command-id <上一轮的commandId>`
       3. 再调用command模式保存当前轮用户输入：`node 能力/保存指令.mjs "当前轮用户原话"`，拿到新的commandId
     - 这一轮的AI回复等**下一轮开始时**再存（滞后存储）
     
     **关键规则：**
     - 同一轮对话的command和reply序号相同（如command-20260911-00001 对应 reply-20260911-00001）
     - 用户在AI回复前连续发多条消息时，**合并成一条command保存**（本质是同一条信息的补充或修改）
     - reply的内容就是上下文里上一轮的回复文本，**AI能从上下文里拿到什么就存什么，不编造，不存AI看不到的二进制内容**（图片/图表等只存工具返回的文本描述和路径）
     - 定时任务触发的：加 `--source system --trigger scheduled` 参数
     - 工具内部自动完成：①用 `时区.mjs` 取本地时间并请求 Gitee 远程时间校验（偏差超30秒用远程时间）；②生成ID；③写入当月 `聊天记录-YYYY-MM.jsonl`；④返回生成的 ID 和完整记录
     - **拿到当前轮的 commandId 后，必须填入后续所有带时间记录的 `data.commandId` 字段**（提交时强制校验 commandId 存在且有效）。这是追溯和防丢失的关键，也是时间可信度的锚点。
  1. **【写入前强制读取，违反则提交必失败】**：禁止在未完成以下读取前写入任何数据：
     - 禁止不读对应类型的「*：类型配置.json」
     - 禁止不读目标文件最近5条完整记录（必须用Read工具，禁止用grep/head/cat搜片段代替——只返回匹配行会丢失meta、关联字段、备注等上下文）
     - 禁止不读用户规则.json就录入带时间的记录（检查是否有该物品/场景的标准录入格式）
     - 禁止凭想象写字段名、物品名、单位名
     - 涉及关联时，禁止不读关联对象的完整文件就写关联字段
  2. **生成ID（强制）**：调用 `node 能力/数据工具.mjs nextid <类型key>` 获取新记录的唯一ID，**禁止凭想象分配ID**。工具自动读取该类型存储文件，找当天最大序号+1。新建类型时必须先创建类型配置（*：类型配置.json），再调用此工具；工具返回"类型未注册"时说明配置没建好，先建配置再生成ID。
  3. **立即写入**：用上一步生成的ID写入对应文件，不得只口头回应"好的"。字段优先复用历史数据中已出现的，新字段谨慎添加。
  4. **【提交前强制自检，禁止跳过】**：禁止未完成字段复用自检就提交：
     - 禁止创建历史数据中不存在的新字段而不做"无法复用论证"（必须找一个最接近的历史字段，说明为什么它不能覆盖本次需求；说明新字段的含义、格式、取值范围；判断以后还会用到吗）
     - 禁止把一次性信息建为独立字段，必须放进note或tags
     - 禁止不拿历史记录与新记录并排比较就提交（确认字段名、格式一致；不一致的必须说明原因）
     - 禁止漏关联、数值/时间不合理、分类错误——有问题立即修复再提交
  5. **同步关联**：按§2关联规则（动态，不预设），判断是否需要关联到人物/项目/活动等；需要关联时双向同步，对端没有反向字段时直接添加。
  6. **使用统一提交工具**：执行 `node 能力/提交并确认.mjs "提交信息"`，工具自动跑综合检查（check=结构+关联），不通过阻止提交，返回 git确认 + 最近一条用户指令
  7. **确认提交结果**：工具返回确认信息即完成；如校验不通过，按错误提示修复后重新提交。
  全部完成才算"记录完成"。
* **【查询铁律】查询类问题必须全量召回**：用户问"今天有什么X""查一下X""X有哪些"等查询问题时，必须用 `node 能力/数据工具.mjs query <类型> --from <日期>` 获取时间范围内的全部记录，禁止只 Read 文件前5条就回答，禁止 grep 搜关键词后只看匹配行就回答。遗漏记录比多花一次工具调用严重得多。
* **【ID铁律】禁止凭想象分配ID**：创建任何新记录前必须调用 `node 能力/数据工具.mjs nextid <类型key>` 获取ID。ID格式为 `<类型key>-<YYYYMMDD>-<5位序号>`，序号由工具自动生成。新建类型时先建类型配置再生成ID。违反此规则会导致ID重复或跳号。
* **【命名铁律】所有新建文件/文件夹必须用中文"名称：备注"格式**：禁止用ID或英文命名文件。一级文件夹=`名称：备注`；子文件夹=`父文件夹简称：子文件夹名`（如`收集箱：活跃`）；单个JSON文件=`显示名：备注.json`（如`父亲：家人.json`、`浇水-换水：文竹.json`）；JSONL数据文件用中文名。文件内容里的ID字段仍用`类型-日期-序号`格式（由nextid工具生成），与文件名分离。搜索人物/项目/任务时直接搜中文文件名，禁止按ID文件名搜索。
* **【兜底规则】无法匹配数据类型时**：用户输入的信息无法匹配任何现有数据类型时，先存入收集箱（inbox）临时存放。收集箱是临时中转，不是最终归宿。后续判断是归入现有类型还是新建类型，新建类型必须遵循第7节"新增数据类型流程"，不得只建文件夹不建类型定义。
* **禁止自行推测缺失字段**：用户没说的字段（如结束时间、金额、分类），留空或标注"待补充"。特别是 activity 的 endTime：用户没说结束时间就必须留空，新活动开始不等于旧活动结束。
* **批量导入、修改历史数据、删除数据**：必须先向用户整理展示"变更计划"，明确等待用户确认；用户确认后才真正写入。
* **任何修改必须立即同步到 Gitee 远程仓库**：改动后即刻提交并推送，不额外询问用户"要不要同步"。
* **提交前必须校验（机器强制）**：commit时自动执行 `node 能力/数据工具.mjs check`（结构+关联综合检查），有 ✗ 先修复再提交。
* **用户个人操作习惯以 `用户规则.json` 为准**：AI发现用户有新偏好或纠正错误时，应主动更新 用户规则.json。
* **【文档维护铁律】本文件保持精简，禁止膨胀**：
  - 新增规则前必须先全文搜索是否已有类似规则，有则更新旧规则，不重复添加
  - 修改任何章节后必须验证：无重复内容、无矛盾字段、引用的章节号正确
  - 类型字段以各数据文件夹内的「*：类型配置.json」为唯一权威，本文件只列核心字段概览，不复制完整定义
  - 具体模板/规范放在独立文件（能力/报表模板.md、能力/可视化规范.md），本文件只引用不展开

### 3.1 负向红线（禁止事项，违反即出错）

以下是最容易犯的错误，**每条都是"不得做"的硬边界**：

| 编号 | 禁止事项 | 正确做法 |
|---|---|---|
| N01 | 禁止用服务器UTC时间或裸`date`猜时间 | 所有时间以用户消息时间为准，统一`+08:00` |
| N02 | 禁止简单append到JSONL文件末尾 | 按time字段排序插入正确位置 |
| N03 | 禁止未经过字段复用自检就写新字段 | 先查历史数据，论证无法复用后再创建新字段 |
| N04 | 禁止脑补、补0、四舍五入数值 | 数值一律取自工具输出或用户原话，缺则留空 |
| N05 | 禁止只写单向关联不更新反向 | 任何关联字段写入时必须同步对端反向字段 |
| N06 | 禁止静默修复后不报告 | 自动修复的每一项必须在回复中列出 |
| N07 | 禁止修复方式不唯一时强行自动修复 | 拿不准就归"需用户确认"，列出选项等决策 |
| N08 | 禁止在渲染器里硬编码标签/单位/配色 | 一律从类型定义和可视化规范读取 |
| N09 | 禁止改动日报里的数字/日期/板块顺序 | 措辞可微调，结构和数据锁死 |
| N10 | 禁止直接git add/commit/push | 必须走`提交并确认.mjs` |

***

## 4. 字段规范

### 4.1 ID 统一规范

所有类型 ID 一律用 `<类型前缀>-<录入日期YYYYMMDD>-<5位序号>`，如 `activity-20260903-00824`、`task-20260904-00001`、`project-20260903-00001`、`person-20260903-00004`、`inbox-20260903-00001`。**禁止用描述性文字做 ID**，名字存在 `name`/`title`/`content` 字段里。

### 4.2 常见字段说明（历史数据中实际存在的，不是强制预设）

以下是历史数据中高频出现的字段，供参考，不是强制要求。新字段经过字段复用自检论证后可以添加：

* `categoryL1` = 一级内容分类（这是什么），单值，自由文本但保持一致性
* `categoryL2` = 二级内容分类，单值
* `form` = 形态分类（如 intake:drink/food）
* `tags` = 标签（和什么相关），多值，逗号分隔
* `status` = 状态，各类型有自己的枚举
* `recurrence` = 调度规则（仅 task），决定到期计算，工具依赖此字段，不能改名

### 4.3 字段命名规范（强制）

完整字段定义、禁止别名、字段含义以各数据文件夹内的「*：类型配置.json」为准。**禁止不读该类型的类型配置就写入任何记录**，必须按其中定义的字段名和约束写入，禁止凭想象写字段名或别名。类型配置是字段规范的唯一权威来源，AGENTS.md 不重复列举具体类型的字段细节。

### 4.4 时间与数值规范

* 时间统一 ISO 8601 + 本地时区（`+08:00`）
* 服务器默认时区是 UTC，**禁止直接用裸 `date`**，必须用 `TZ=Asia/Shanghai date`
* 数值存数字，字符串原样保留（含空串、异常值如 0 元）
* **禁止用服务器时间或自行猜测时间**，所有录入的时间以用户消息的时间为准，除非用户明确提到了具体时间

#### 4.4.1 时间溯源双字段（commandId + timeSource，强制）

每条带时间的新增记录必须同时填写两个字段（放在 `data` 容器内），形成完整溯源链：

| 字段 | 含义 | 验证方式 |
|---|---|---|
| `commandId` | 用户原始语句的 ID（通过 `保存指令.mjs` 获取） | 提交时强制验证该 ID 存在于聊天记录中，不存在则阻止提交 |
| `timeSource` | 时间值的具体来源 | 根据来源类型分别验证（见下） |

`timeSource` 有三种写法：

1. **`timeSource = commandId`（时间从用户语句解析）**
   - 适用：用户说"明天下午6点"、"刚刚"、"现在"等，时间值是从用户原话里解析或推断的
   - 验证：只验证 commandId 存在（自然语言无法自动匹配时间值，靠人工核对）

2. **`timeSource = 其他记录ID（时间从历史记录衔接）**
   - 适用：时间值来自另一条记录的时间字段，如散步结束=工作开始（`timeSource: "activity-20260907-00003"`）、任务到期日（`timeSource: "task-20260904-00001"`）
   - 验证：提交时自动提取该记录的时间字段（startTime/endTime/time/dueDate等），与当前记录时间比对（±60秒），不匹配则阻止提交

3. **`timeSource = "user:xxx"（用户口述说明）**
   - 适用：时间是用户口头说明但没有可引用的记录ID，如 `timeSource: "user:昨天晚上完成了"`
   - 验证：只要求非空，不做自动匹配，靠人工核对

**示例**：
```json
{
  "id": "activity-20260907-00004",
  "data": {
    "startTime": "2026-09-07T12:39:11+08:00",
    "activity": "个人数据管理系统开发",
    "commandId": "command-20260907-00001",
    "timeSource": "activity-20260907-00003"
  }
}
```
含义：这条工作记录的用户原始语句是 command-20260907-00001，开始时间 12:39:11 来自散步记录 activity-20260907-00003 的结束时间。

### 4.5 JSONL 文件排序规则（强制）

所有 JSONL 数据文件必须按 `time` 字段**倒序排列**：最近的在最上面。
* 主排序键：`data.time` 或 `data.startTime`，倒序（新→旧）
* 次排序键：同一天按 `id` 正序
* **禁止简单追加到文件末尾**，新记录必须根据 time 插入到对应位置
* 修改记录的 time 后必须重新排序整个文件
* **禁止用ID中的日期做查询/排序/统计**，ID中的日期是录入日期，不是事件时间，一律按 time 字段

## 5. 任务 Task 规范

### 5.1 任务类型与状态

* **所有 task 都是提醒类任务（reminder）**，有固定到期时间或重复规则。
* **禁止创建 type=project 的 task**，长期项目（有milestones、需聚合数据）必须使用独立的 project 类型。
* `status`：`active`（活跃）/ `completed`（已完成）/ `cancelled`（已取消）/ `paused`（暂停）
* 活跃任务以单 JSON 文件存在于 `数据/待办任务：有到期时间或重复规则的提醒任务/`，归档任务统一追加到 `数据/待办任务：有到期时间或重复规则的提醒任务/待办任务：已归档/已完成.jsonl`

### 5.2 调度规则（recurrence）

合法值：`one_time`（一次性）、`daily`（每天）、`days`（间隔天数）、`monthly`（每月）、`interval`（每N月）、`seasonal`（按季节）、`cycle`（按周期数组）、`dates`（按日期数组）。`once` 作为 `one_time` 的别名也接受。

**到期计算唯一权威是 `能力/调度.mjs`**。生成安排板块前必须跑 `node 能力/数据工具.mjs due <YYYY-MM-DD>` 拿清单，**禁止自行推算**。

### 5.3 任务归档规范（强制）

**禁止手动分步归档任务**，必须使用一键归档工具：
```bash
node 能力/归档任务.mjs <task_id> [--time "YYYY-MM-DDTHH:mm:ss+08:00"] [--category "日常"] [--note "备注"] [--dry-run]
```
工具自动执行完整影响链（见第2.4节），任何一步失败自动回滚。手动归档必漏反向关联。

**归档触发条件（必须同时满足）**：
1. 用户明确表示任务已完成
2. 能匹配到唯一的任务文件
3. 任务类型可归档（one_time 或 daily）

**防止错误判断的铁律**：
1. 匹配不到任务就不归档，禁止凭猜测归档
2. 匹配到多个必须询问，列出所有匹配任务让用户选择
3. 周期性任务（monthly/seasonal/interval/days/cycle/dates）只创建 activity 记录，不修改任务 status
4. ~~项目任务不归档~~（已取消 type=project，长期项目用独立 project 类型管理）
5. 取消任务（≠完成）：不创建 activity 记录，更新 status=cancelled，追加到 completed.jsonl 并删除原文件

***

## 6. 项目 Project 与人物 People

### 6.1 项目 Project

**项目是系统里唯一的长期项目类型**，承担两个职责：
1. **跨数据类型聚合**：所有实际数据通过 `projectId` 字段关联到项目
2. **进度追踪**：项目有 `milestones`（里程碑）、`status`（进行中/已完成/暂停）、`startDate`/`endDate`

* 什么时候用：长期项目、持续计划、需要追踪进度和历程的事项
* 存储：`数据/我的项目：长期项目或者主题任务/<id>.json`（单JSON文件）
* 一条记录只能关联一个项目（`projectId` 是单值）
* 反向关联：project.activityIds、project.taskIds、project.inboxIds
* **禁止在 task 里创建 type=project 的任务**，长期项目统一用 project 类型

### 6.2 人物 People

人物是独立实体，所有记录通过 `peopleIds` 字段关联（多人用逗号分隔）。

**【创建人物前强制检查，禁止跳过】**：
1. 禁止不列出 `数据/我认识的人：我认识的所有人物/` 目录下所有人物档案就创建新人物
2. 禁止不按名称和关系模糊匹配就创建新人物
3. 已存在则复用已有 ID，**禁止重复创建人物**

* `relation`（单数）= 和我的关系，字符串（如"父亲""女儿"）
* `relations`（复数）= 和其他人的关系，数组（如 `[{personId, relation}]`）
* 两者含义不同，不统一
* **人物自己的事件**（生日、工作经历、旅行等与我无关的事）存在人物 JSON 的 `events` 数组里，不写 activity
* **我和人物一起的事件**（一起吃饭、一起旅行）写 activity，通过 `peopleIds` 关联人物
* 判断标准：这句话的主语是谁？主语是"我"→activity，主语是别人→person.events

***

## 7. 新增数据类型流程

新增一个数据类型必须按以下顺序完成，**每一步完成后必须验证，不能只做不查**：

1. **创建数据存储**：在 `数据/<中文名称：备注>/` 下创建 `<中文名称>.jsonl` 文件（空文件即可）。**文件夹和文件名必须用中文"名称：备注"格式，禁止用英文或ID命名**
   - 验证：`ls 数据/<中文名称：备注>/` 确认文件存在
2. **创建类型配置**：在对应的数据文件夹内创建「<中文名称>：类型配置.json」，包含key、name、description、storage、usesDataWrapper、核心约束、排序规则。**类型配置文件名必须和文件夹名称一致**
   - 验证：`cat 数据/<对应文件夹>/<类型名>：类型配置.json` 确认JSON合法
3. **无需类型目录**：类型定义直接放在各数据文件夹内，工具自动扫描发现，不需要单独的类型目录文件
   - 验证：`node 能力/数据工具.mjs types` 确认类型已注册
4. **无需更新 AGENTS.md**：类型定义直接放在各数据文件夹内，工具自动扫描发现，不需要在 AGENTS.md 里维护类型清单或目录树
   - 验证：`node 能力/数据工具.mjs types` 确认新类型已被扫描发现
5. **如需要双向关联**：在 `能力/数据检查/关联检查.mjs` 里添加新类型的关联检查规则，并在第2节操作影响矩阵里添加对应行
   - 验证：`node 能力/数据检查/关联检查.mjs` 确认无遗漏
6. **如需要渲染**：在 `能力/渲染今日.mjs` 里添加新类型的渲染逻辑
   - 验证：`node 能力/渲染今日.mjs --mode morning` 确认能渲染
7. **最终验证**：运行 `node 能力/数据工具.mjs check` 确认无错误，然后提交

字段可随时追加，不影响历史数据。

***

## 8. 批次导入（保证零丢失零错误的流程）

1. 拿到源表后**先读表头**，列出字段映射计划，试跑 5~10 条给用户确认映射正确。
2. 确认后**分块导入**，非一次梭哈。
3. **双重校验**：条数一致、金额合计一致、逐字段比对无差异；以源表为基准核对。
4. 同一天多条取最晚一次；重复项去重（都需向用户说明并经确认）。

***

## 9. 每日汇报与可视化
* **到期清单唯一来源**：`node 能力/数据工具.mjs due <YYYY-MM-DD>`，禁止自行推算。
* 汇报内容/格式以 `用户规则.json` + `能力/报表模板.md` 为准，本文件不展开。
* 可视化遵循 `能力/可视化规范.md`，默认内联呈现，用户要求才导出文件。
* **禁止臆造数据**；有不明确处先问用户。**禁止未经用户指示就清理、改写、丢弃字段**。

### 9.1 晚报自动总结板块（强制，晚报必须包含）

**【晚报强制板块，禁止省略】**晚报末尾必须包含「今日未记录信息建议」板块，用于自动发现聊天记录里提到了但还没录入的数据。

**执行流程（禁止跳过任何一步）：**
1. **禁止不读当天聊天记录就生成建议**：用 `node 能力/数据工具.mjs query command --from <今天日期> --to <今天日期>` 获取当天所有聊天记录（含command和reply）
2. **禁止不读当天已记录数据就生成建议**：对每个数据类型分别调用 `node 能力/数据工具.mjs query <类型key> --from <今天日期>`，获取当天所有已录入的记录
   - 一个用户一天主动录入的信息最多十几二十条，全部查出来对比，**禁止用grep搜关键词匹配**
3. **对比分析**：找出"聊天记录里提到了、但已记录数据里没有"的信息
4. **生成建议清单**，分三类：
   - **可以补充到现有类型的**：比如用户说喝了水但没记录摄入，建议补充一条摄入记录
   - **建议新增类型的**：比如用户反复提到情绪但没有情绪记录类型，建议新增一个"每日情绪"数据类型，并把今天的情绪填好
   - **可能遗漏的重要信息**：比如用户提到一个待办但没创建任务，建议创建任务
5. **每条建议必须带**：
   - 建议内容（具体要记录什么）
   - 建议记录到哪个类型（现有类型或建议新增的类型名称）
   - 对应的聊天记录ID（commandId，方便追溯）
6. **用户倾向性建议**：如果从聊天记录里发现用户有明确的沟通偏好或操作习惯（如用户多次要求"简洁回复"），建议写入 `用户规则.json`
7. **禁止自动记录建议内容**：这些只是建议，**必须等用户确认后才记录**。用户可以说"采纳第1条"、"第2条改成XXX"、"第3条不用了"等

**输出格式参考：**
```
## 今日未记录信息建议

1. [补充现有类型] 建议补充一条摄入记录：下午3点喝了一瓶矿泉水500ml
   - 建议记录到：饮食摄入
   - 聊天记录ID：command-20260911-00005

2. [建议新增类型] 用户今天多次提到"很累""压力大"，建议新增"每日情绪"数据类型，今天的情绪为"疲惫"
   - 建议记录到：每日情绪（新增）
   - 聊天记录ID：command-20260911-00008

3. [用户规则建议] 用户今天3次要求"简洁说明"，建议写入用户规则：回复尽量简洁明了
   - 建议更新：用户规则.json
   - 聊天记录ID：command-20260911-00003, command-20260911-00006, command-20260911-00009
```

***

## 10. 动态规则捕获（强制）

**核心思想：用户的偏好和风格在对话中自然浮现，Agent 应当主动捕捉并沉淀为规则。**

**规则分两处存放：**
* **用户个人操作习惯/偏好** → 存入 `用户规则.json`（录入习惯、汇报偏好、预算、可视化等）
* **系统级规则/行为约束** → 追加到本文件（AGENTS.md）

### 10.1 触发时机
当 Agent 察觉到用户出现以下任一情况时，**必须主动询问"要不要把这个更新到规则里？"**：
* 对输出的格式、布局、呈现方式有明确意见
* 对数据的记录方式、分类方式有偏好倾向
* 对沟通方式、交互节奏有自己的习惯
* 纠正了 AI 的操作错误（如"你怎么没记摄入"）
* 任何可能影响今后工作方式的个人风格表达

### 10.2 操作流程
1. Agent 主动提问："我注意到你倾向于…… 要不要把这个更新到规则里？"
2. 用户确认后，判断规则类型：用户个人习惯→更新 `用户规则.json`；系统级规则→追加到本文件
3. 通过 `提交并确认.mjs` 提交并 push 到 Gitee 远程仓库
